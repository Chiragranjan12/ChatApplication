spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.nexus.chat: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
