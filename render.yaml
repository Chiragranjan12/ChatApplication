services:
  - type: web
    name: nexus-chat-backend
    env: java
    rootDir: server
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/*.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
