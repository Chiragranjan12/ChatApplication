# Development environment — values here are only active during `vite dev`
# Auth bypass and verbose logging are automatically active when VITE_DEV=true (import.meta.env.DEV)
VITE_API_BASE_URL=http://localhost:5000
