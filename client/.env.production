# Production environment — fill these values before deploying
# The auth bypass and STOMP debug logs are automatically disabled in production
# because import.meta.env.DEV === false at build time.

# Set to your actual production API origin (no trailing slash)
# If you serve the API and client from the same origin, leave this empty.
VITE_API_BASE_URL=https://chatapplication-6jyl.onrender.com
